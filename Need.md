## Что нужно?
- БПЛА самолетного типа с ДВС и размахом крыльев от 2.5 метров
- оператор, который умеет и может управлять полетом БПЛА днем и ночью
- полигон длиной не менее 15 км
- видео камеры с большим зумом
- MWIR и LWIR тепловизоры
- возможность управлять зумом на камерах и тепловизорах
- повортная тренога
- возможность записывать видео с камеры и тепловизоров

## На какие вопросы необходимо уметь отвечать?

1. Камера с какими характеристиками (размер матрицы, зум, угол обзора, фокусные расстояния) нужна для обнаружения БПЛА с размахом крыльев от 2.5 метров в хорошую погоду с помощью ИИ?
2. Тепловизор с какими характеристики (тип камеры, чувствительность, размер матрицы, зум, угол обзора) нужен для обнаружения БПЛА с размахом крыльев от 2.5 метров и ДВС с помощью ИИ?
3. Сколько пикселей на изображении c высотой H и шириной W будет занимать БПЛА с линейными размерами X на расстоянии Y, если используется камеры с фокусным расстоянием F?

Пример характеристик камеры
- Разрешение - 1920х1080, Full HD
- Фокусное расстояние объектива - От 27 мм до 1070 мм (зум 40х)
- Угловое поле зрения - От 15,4°(Г)х8,7°(В) до 0,45°(Г)х0,25°(В)

Пример характеристик тепловизироа
- Спектральный диапазон - 3-5 мкм (MWIR)
- Разрешение детектора - 640х512
- Размер пикселя детектора - 15 мк
- Температурная чувствительность - 17 мк (NETD)
- Частота кадров - 25 fps
- Фокусное расстояние объектива - От 21 мм до 420 мм (зум 20х)
- Угловое поле зрения - От 25,7°(Г)х20,7°(В) до 1,31°(Г)х1,05°(В)

- Размер БПЛА (X) - 3 м
- Расстояние до БПЛА (Y) - 5000 м
- Фокусное расстояние (F1) - 0.027 м, F2 = 1.07 м
- L1 = (X*F1)/Y = 0.0000162 м
- L2 = (X*F2)/Y = 0.000642 м


Конечно! Вот пример кода на Python, который вычисляет размер БПЛА в пикселях на изображении, используя заданные параметры:

def calculate_drone_size_in_pixels(X, Y, F, W, H, sensor_width, sensor_height):
    # Углы поля зрения (в радианах)
    fov_width = 2 * (sensor_width / (2 * F))  # Угол по ширине
    fov_height = 2 * (sensor_height / (2 * F))  # Угол по высоте

    # Размер БПЛА на изображении
    size_on_image = (X * F) / Y

    # Масштаб изображения
    scale_width = W / fov_width
    scale_height = H / fov_height

    # Количество пикселей, занимаемых БПЛА
    pixels_x = size_on_image * scale_width
    pixels_y = size_on_image * scale_height

    return pixels_x, pixels_y

# Пример использования:
X = 1.0  # линейные размеры БПЛА (например, 1.0 метр)
Y = 100.0  # расстояние до БПЛА (например, 100.0 метров)
F = 50.0  # фокусное расстояние (например, 50.0 мм)
W = 1920  # ширина изображения в пикселях (например, 1920 пикселей)
H = 1080  # высота изображения в пикселях (например, 1080 пикселей)
sensor_width = 0.036  # ширина сенсора (например, 36 мм)
sensor_height = 0.024  # высота сенсора (например, 24 мм)

pixels_x, pixels_y = calculate_drone_size_in_pixels(X, Y, F, W, H, sensor_width, sensor_height)
print(f"БПЛА занимает {pixels_x:.2f} пикселей по ширине и {pixels_y:.2f} пикселей по высоте.")
В этом коде:
- Задаются размеры БПЛА, расстояние до него, фокусное расстояние и размеры изображения.
- Вычисляются углы поля зрения, размеры БПЛА на изображении и количество пикселей, которые он занимает.
- Результат выводится на экран с форматированием до двух знаков после запятой. 
